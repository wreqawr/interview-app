server:
  port: 80
spring:
  application:
    name: interview-gateway
  banner:
    location: classpath:banner/banner.txt

  # nacos配置中心
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR}
      discovery:
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: interview
        group: ${NACOS_GROUP_NAME}

    gateway:
      server:
        webflux:
          routes:
            - id: user-service
              uri: lb://user-service
              predicates:
                - Path=/api/user/**

            - id: resume-service
              uri: lb://resume-service
              predicates:
                - Path=/api/resume/**

            - id: ai-service
              uri: lb://ai-service
              predicates:
                - Path=/api/ai/**

            - id: candidate-service
              uri: lb://candidate-service
              predicates:
                - Path=/api/candidate/**

            - id: interview-service
              uri: lb://interview-service
              predicates:
                - Path=/api/interview/**


#logging:
#  level:
#    org.springframework.boot.autoconfigure: DEBUG
#    org.springframework.cloud.commons.config: DEBUG
#    com.alibaba.cloud.nacos: DEBUG