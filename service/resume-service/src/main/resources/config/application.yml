server:
  port: 20000

spring:
  application:
    name: resume-service
  banner:
    location: classpath:banner/banner.txt

  # nacos配置中心
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR}
      discovery:
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: interview
        group: dev
      config:
        namespace: ${spring.cloud.nacos.discovery.namespace}
        group: ${spring.cloud.nacos.discovery.group}
    openfeign:
      client:
        config:
          default:
            connectTimeout: 120000
            readTimeout: 120000
            logger-level: full
  config:
    import:
      - nacos:nacos-${spring.application.name}.yml?refreshEnabled=true
      - nacos:nacos-mysql.yml?refreshEnabled=true
      #      - nacos:nacos-redis.yml?refreshEnabled=true
      - nacos:nacos-redis-local.yml?refreshEnabled=true
      - nacos:nacos-mongo.yml?refreshEnabled=true
