# Interview App Docker 部署快速说明

## 主要变更

1. ✅ **仅包含微服务**: docker-compose.yml 中移除了所有中间件（MySQL、Redis、MongoDB、MinIO、Nacos），仅保留 6 个微服务
2. ✅ **Maven 镜像加速**: 所有 Dockerfile 已配置使用阿里云 Maven 镜像，加速依赖下载
3. ✅ **外部服务配置**: 通过环境变量 `NACOS_SERVER_ADDR` 配置 Nacos 地址

## 快速部署

### 1. 配置 Nacos 地址

```bash
# 方式一：环境变量
export NACOS_SERVER_ADDR=your-nacos-host:8848

# 方式二：创建 .env 文件
echo "NACOS_SERVER_ADDR=your-nacos-host:8848" > .env
```

### 2. 构建镜像

```bash
# 构建所有服务（使用阿里云 Maven 镜像，速度更快）
./build-docker.sh

# 或构建单个服务
./build-docker.sh gateway
```

### 3. 启动服务

```bash
# 启动所有微服务
docker-compose up -d

# 查看日志
docker-compose logs -f gateway
```

## 服务端口

- Gateway: 80
- User Service: 10000
- Resume Service: 20000
- Interview Service: 30000
- Candidate Service: 40000
- AI Service: 50000

## 注意事项

1. **外部服务**: 确保 MySQL、Redis、MongoDB、MinIO、Nacos 等中间件已部署并可访问
2. **网络配置**: 如果服务在不同主机，确保网络可达
3. **Nacos 配置**: 数据库连接等配置需要在 Nacos 配置中心配置

## 详细文档

更多详细信息请参考 `DOCKER.md`

