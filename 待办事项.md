## 待办事项：

1. 简历上传、删除失败时对应的事务回滚策略。
2. 简历分析接口，通过三级缓存获取简历分析结果，需要防止缓存穿透。
3. 简历获取接口getMyResume直接从mysql获取，可以考虑进行缓存（已完成）。
    - 对简历信息的修改操作，需要考虑数据最终一致性（延时双删）。
4. 用户注册去重，目前采用的是mysql唯一键机制，可以考虑使用布隆过滤器。
5. 用户注册权限控制，会先将所有权限列表从mysql加载至redis进行缓存预热（只读），但是需要考虑数据一致性问题。
6. openFeign远程调用失败时的服务降级策略（兜底返回）。
7. 接口限流（网关层、资源层）。
8. ai接口调用次数限制，防止恶意刷请求。
9. 性能优化。