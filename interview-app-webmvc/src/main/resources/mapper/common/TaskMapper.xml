<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.minglg.interview.common.mapper.TaskMapper">

    <insert id="initTask">
        insert into t_task(id, user_id, task_id, task_type, task_status, method_name, method_args, start_time,
                           end_time, error_message)
        values (null, #{task.userId}, #{task.taskId}, #{task.taskType}, #{task.taskStatus}, #{task.methodName},
                #{task.methodArgs},
                #{task.startTime}, #{task.endTime}, #{task.errorMessage})
    </insert>

    <update id="updateTask">
        update t_task
        set task_status=#{task.taskStatus},
            end_time=#{task.endTime},
            error_message=#{task.errorMessage}
        where user_id = #{task.userId}
          and task_id = #{task.taskId}
    </update>

    <select id="getTask" resultType="cn.minglg.interview.common.pojo.Task">
        select id,
               user_id,
               task_id,
               task_type,
               task_status,
               method_name,
               method_args,
               start_time,
               end_time,
               error_message
        from t_task
        where user_id = #{task.userId}
          and task_id = #{task.taskId}
    </select>


</mapper>